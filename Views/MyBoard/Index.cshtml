@model event_web_dev_project.Models.MyBoardViewModel
@{
    ViewData["Title"] = "My Board";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link rel="stylesheet" href="~/css/myboard.css" />


<div class="myboard-container">
    <div class="myboard-header">
        <h1 class="myboard-title">My Board</h1>
        <p class="myboard-subtitle">Manage your posts and track your applications</p>
    </div>

    <!-- Tab Navigation -->
    <div class="tab-nav" id="tabNav">
        <button class="tab-btn active" data-tab="posts">
            My Posts <span class="tab-count" id="postsCount">@Model.PostsTab.TotalPosts</span>
        </button>
        <button class="tab-btn" data-tab="applications">
            My Applications <span class="tab-count" id="appsCount">@Model.ApplicationsTab.TotalApplications</span>
        </button>
        <button class="tab-btn" data-tab="invitations">
            Invitations <span class="tab-count" id="invitesCount">@(Model.InvitationsTab.SentTotalInvitations+Model.InvitationsTab.ReceivedTotalInvitations)</span>
        </button>
    </div>

    <div class="tab-divider"></div>

    <!-- Tab Panels -->
    <div class="tab-panel active" id="tab-posts">
        @await Html.PartialAsync("~/Views/MyBoard/Components/_MyPostsTab.cshtml", Model.PostsTab)
    </div>

    <div class="tab-panel" id="tab-applications">
        @await Html.PartialAsync("~/Views/MyBoard/Components/_MyApplicationsTab.cshtml", Model.ApplicationsTab)
    </div>

    <div class="tab-panel" id="tab-invitations">
        @await Html.PartialAsync("~/Views/MyBoard/Components/_InvitationsTab.cshtml", Model.InvitationsTab)
    </div>
</div>

<script src="~/js/myboard.js"></script>
